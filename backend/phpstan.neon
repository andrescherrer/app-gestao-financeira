includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    paths:
        - app

    # Rule level (0-9, higher is stricter)
    level: 5

    excludePaths:
        - ./*/*/FileToIgnore.php

